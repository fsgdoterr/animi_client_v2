import { Cuprum, Didact_Gothic } from 'next/font/google'

export const cuprum = Cuprum({
    subsets: ['cyrillic','latin'],
});

export const didactGothic = Didact_Gothic({
    weight: '400',
    subsets: ['cyrillic','latin'],
});